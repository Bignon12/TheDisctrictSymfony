{% extends 'base.html.twig' %}

{% block body %}

<div class = "container">
    <div>
        <h1 class="text-center m-4">Voici les plats de cette catégorie</h1>
    </div>

    <div id="aff" class="row d-flex justify-content-around mx-auto">
        {% for element in plat %}
        <div class="card mb-3" style="max-width: 540px; background-color: #ff66c4">
            <div class="row g-0">
                <div class="col-sm-4">
                    <img src= "{{ asset('images_the_district/food/' ~ element.image) }}" class="img-fluid cover rounded-start" alt="{{ element.libelle }}">
                </div>
                <div class="col-sm-8">
                    <div class="card-body">
                        <h3 class="card-text text-center" value = {{element.id}}{{element.libelle}}>{{ element.libelle }}</h3>
                        <p>{{element.description}}</p>
                        <p>{{element.prix}}€</p>
                        <a href="{{ path('app_catalogue',{'id': element.id }) }}">
                            <svg xmlns="http://www.w3.org/2000/svg" width="50" height="50" fill="currentColor" class="bi bi-cart-dash" viewBox="0 0 16 16">
                                <path d="M6.5 7a.5.5 0 0 0 0 1h4a.5.5 0 0 0 0-1z"/>
                                <path d="M.5 1a.5.5 0 0 0 0 1h1.11l.401 1.607 1.498 7.985A.5.5 0 0 0 4 12h1a2 2 0 1 0 0 4 2 2 0 0 0 0-4h7a2 2 0 1 0 0 4 2 2 0 0 0 0-4h1a.5.5 0 0 0 .491-.408l1.5-8A.5.5 0 0 0 14.5 3H2.89l-.405-1.621A.5.5 0 0 0 2 1zm3.915 10L3.102 4h10.796l-1.313 7zM6 14a1 1 0 1 1-2 0 1 1 0 0 1 2 0m7 0a1 1 0 1 1-2 0 1 1 0 0 1 2 0"/>
                            </svg>
                        </a>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

{% endblock %}